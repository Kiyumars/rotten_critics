Demoing an API call to themoviedb.org and create an actor model.

Enter the name of the actor. <br>
<%= form_tag(create_actor_path) do %>
  <%= label_tag :actor_name, "Name of actor:" %>
  <%= text_field_tag :actor_name, nil, placeholder: "Al Pacino (for example)" %>
  <%= submit_tag "Submit actor name" %>
<% end %>

<br><br>
Sessions Demo
<%= link_to "Click here", new_session_path %>