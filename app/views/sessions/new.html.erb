<p>Let's play a game. Enter the names of all players and the name of the actor or actress.</p>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:session, url: create_session_path) do |f| %>

      <%= f.label :players %>
      <%= f.text_field :players, class: 'form-control', placeholder: "Tom, Dick, Harry" %>

      <%= f.label :actor %>
      <%= f.text_field :actor, class: 'form-control', placeholder: "Al Pacino" %>

      <%= f.submit "Start the game", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>