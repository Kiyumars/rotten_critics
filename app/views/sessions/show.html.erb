A sessions cookie has been created. <br>

Now guess what percentage of critics on rottentomatoes liked <i>Guardians of the glaxy</i> <br>
<%= form_for(:players_guesses, url: score_update_path, method: :patch) do |f| %>
  <% session[:players_names].each do |player| %>
    <%= f.label player %>
    <%= f.text_field player, value: "42", class: 'form-control', placeholder: "42" %>
    <% end %>
    <%= f.submit "Update the score", class: "btn btn-primary" %>
<% end %>



<%= link_to "Log out", destroy_session_path, method: :delete %>